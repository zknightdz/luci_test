import 'package:flutter/material.dart';

class JobAndProject extends StatelessWidget {
  const JobAndProject({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
